# =============================================================================
# requirements.txt — Pin EVERY version to avoid silent breakage between builds
# =============================================================================
# CRITICAL (Osama): Unpinned installs can silently change behaviour between
# versions and break model loading. Always pin exact versions.
# =============================================================================

# Core deep learning framework
torch==2.2.1
torchaudio==2.2.1
torchvision==0.17.1

# Audio processing
librosa==0.10.1
soundfile==0.12.1

# Noise reduction (EL sir)
noisereduce==3.0.2

# Numerical / scientific computing
numpy==1.26.4
scipy==1.12.0

# Data handling and analysis
scikit-learn==1.4.1.post1       # For stratified splits, confusion matrix
pandas==2.2.1                    # For logging results and analysis

# Visualization (Osama — confusion matrix, training curves)
matplotlib==3.8.3
seaborn==0.13.2

# Progress bars
tqdm==4.66.2
